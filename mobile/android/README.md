# AgriApp Android

Planned Android tablet client for LAN control of Raspberry Pi:
- device discovery (manual IP first, optional scan)
- system dashboard (`/api/v1/system/status`)
- capture trigger (`/api/v1/capture/oneshot`)
- gallery read-only (`/api/v1/images`)

This folder is intentionally empty for now and will host the Android Studio project.
